{
  "name": "@merchconnect/features",
  "version": "0.1.0",
  "private": false,
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -w -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint 'src/**/*.{ts,tsx}'",
    "test": "node -e \"console.log('features ok')\""
  },
  "dependencies": {
    "zod": "^3.23.8",
    "@merchconnect/types": "workspace:*"
  }
}
